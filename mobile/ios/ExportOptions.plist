<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  ExportOptions.plist — required by `xcodebuild -exportArchive`

  Fill in YOUR_TEAM_ID with your Apple Developer Team ID (10-character string).
  Find it at https://developer.apple.com/account -> Membership -> Team ID

  method options:
    app-store       — upload to App Store / TestFlight
    ad-hoc          — distribute to registered devices
    development     — internal testing on registered devices
    enterprise      — in-house distribution (requires Enterprise account)
-->
<plist version="1.0">
<dict>
    <key>method</key>
    <string>app-store</string>

    <key>teamID</key>
    <string>YOUR_TEAM_ID</string>

    <key>uploadSymbols</key>
    <true/>

    <key>compileBitcode</key>
    <false/>

    <key>signingStyle</key>
    <string>automatic</string>
</dict>
</plist>
